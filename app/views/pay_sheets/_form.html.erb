<%= form_for(@pay_sheet) do |f| %>
  <% if @pay_sheet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pay_sheet.errors.count, "error") %> prohibited this pay_sheet from being saved:</h2>

      <ul>
      <% @pay_sheet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount %><br />
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label :date %><br />
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :observation %><br />
    <%= f.text_area :observation %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
